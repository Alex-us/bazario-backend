export enum LoggerTags {
  AUTH = 'auth',
  EMAIL = 'email',
  DB = 'db',
  EXPRESS = 'express',
  SERVER = 'server',
  ACCOUNT = 'account',
  REDIS = 'redis',
  USER = 'user',
  NOTIFICATIONS = 'notifications',
}
